# .gitignore 配置说明

本文件说明了GustoBot项目中 `.gitignore` 的配置规则。

## 📋 忽略文件分类

### 1. Python相关 🐍

```gitignore
__pycache__/          # Python字节码缓存
*.py[codz]            # 编译后的Python文件
*.so                  # C扩展
.pytest_cache/        # Pytest缓存
.mypy_cache/          # MyPy类型检查缓存
```

### 2. Node.js / npm 前端依赖 📦

```gitignore
node_modules/         # npm安装的依赖包（不上传）
npm-debug.log*        # npm调试日志
package-lock.json     # npm锁定文件（可选）
yarn.lock             # Yarn锁定文件
pnpm-lock.yaml        # pnpm锁定文件
```

**说明**: `node_modules/` 通常非常大（几百MB），不应该上传到Git。其他开发者通过 `npm install` 即可安装。

### 3. 前端构建产物 🏗️

```gitignore
web/dist/             # Vite构建输出
web/build/            # 其他构建工具输出
web/.next/            # Next.js构建缓存
```

### 4. ONNX 模型文件 🤖

```gitignore
*.onnx                # ONNX模型文件（不上传）
*.onnx.json           # ONNX配置
models/*.onnx         # models目录下的ONNX文件
onnxruntime/          # ONNX Runtime
```

**说明**: ONNX模型文件通常很大（几MB到几GB），不适合上传到Git。应该：
- 使用Git LFS（Large File Storage）
- 或提供下载链接
- 或使用模型托管服务（Hugging Face等）

### 5. 其他机器学习模型 🧠

```gitignore
*.pt                  # PyTorch模型
*.pth                 # PyTorch checkpoint
*.h5                  # Keras/TensorFlow模型
*.pkl, *.pickle       # Pickle序列化文件
*.joblib              # Joblib序列化
*.bin                 # 二进制模型文件
*.safetensors         # SafeTensors格式
```

### 6. IDE配置 💻

```gitignore
.idea/                # IntelliJ IDEA配置（不上传）
*.iml                 # IDEA模块文件
.vscode/settings.json # VS Code个人设置
*.swp, *.swo          # Vim交换文件
```

**说明**: `.idea/` 包含个人IDE设置，每个开发者的配置不同，不应该上传。

### 7. 向量数据库 🗄️

```gitignore
chroma/               # ChromaDB持久化
milvus/               # Milvus持久化
data/chroma/          # ChromaDB数据目录
data/milvus/          # Milvus数据目录
data/vector_store/    # 通用向量存储
```

**说明**: 向量数据库的持久化文件通常很大，且包含业务数据，不应该上传。

### 8. 爬虫相关 🕷️

```gitignore
proxies.txt           # 代理配置（敏感信息）
cookies.json          # Cookie文件（包含登录状态）
*.html                # 爬取的HTML文件（临时）
screenshots/          # 截图目录
downloads/            # 下载目录
crawled_data/         # 爬取的数据
```

**说明**:
- `proxies.txt` 和 `cookies.json` 包含敏感信息，不应该上传
- 爬取的HTML和数据文件应该本地存储或使用专门的存储服务

### 9. 数据文件 📊

```gitignore
data/                 # 数据目录
*.csv                 # CSV数据文件
*.json.gz             # 压缩的JSON
*.parquet             # Parquet格式
*.feather             # Feather格式
*.arrow               # Arrow格式
```

**说明**: 大型数据文件不应该上传到Git，应该：
- 使用数据版本控制工具（DVC）
- 或使用云存储（S3、OSS等）
- 或提供数据下载脚本

### 10. 环境配置与密钥 🔐

```gitignore
.env                  # 环境变量文件（包含密钥）
.env.local            # 本地环境变量
secrets/              # 密钥目录
*.pem, *.key          # 私钥文件
*.crt, *.p12          # 证书文件
```

**说明**: **绝对不能上传密钥和敏感信息！** 应该：
- 提供 `.env.example` 作为模板
- 在README中说明需要配置的环境变量
- 使用密钥管理服务（如AWS Secrets Manager）

### 11. 日志和临时文件 📝

```gitignore
logs/                 # 日志目录
*.log                 # 日志文件
*.log.*               # 轮转日志
tmp/, temp/           # 临时目录
*.tmp, *.temp         # 临时文件
```

### 12. 数据库文件 🗃️

```gitignore
*.db                  # SQLite数据库
*.sqlite              # SQLite数据库
*.sqlite3             # SQLite数据库
```

**说明**: 开发数据库不应该上传，每个开发者应该：
- 使用迁移脚本（Alembic）初始化数据库
- 或使用测试数据脚本填充数据

### 13. 操作系统文件 💾

```gitignore
# macOS
.DS_Store             # macOS目录配置
.AppleDouble          # macOS资源分支
._*                   # macOS元数据

# Windows
Thumbs.db             # Windows缩略图缓存
Desktop.ini           # Windows文件夹配置
$RECYCLE.BIN/         # Windows回收站

# Linux
.directory            # KDE目录配置
.Trash-*              # Linux回收站
```

---

## ⚠️ 重要提醒

### ❌ 不要上传的内容

1. **敏感信息**
   - API密钥、数据库密码
   - 代理配置、Cookie
   - 私钥、证书

2. **大文件**
   - `node_modules/` (可达几百MB)
   - ONNX模型文件 (几MB到几GB)
   - 向量数据库持久化 (几GB)
   - 爬取的数据文件

3. **个人配置**
   - IDE配置 (`.idea/`, `.vscode/settings.json`)
   - 操作系统文件 (`.DS_Store`, `Thumbs.db`)

4. **临时文件**
   - 日志文件
   - 缓存文件
   - 临时下载

### ✅ 应该上传的内容

1. **源代码**
   - Python代码 (`.py`)
   - JavaScript代码 (`.js`, `.jsx`)
   - CSS样式 (`.css`)
   - HTML模板

2. **配置模板**
   - `.env.example` (不包含真实密钥)
   - `proxies.txt.example` (示例格式)

3. **文档**
   - README.md
   - docs/ 文档目录
   - 注释和说明

4. **依赖声明**
   - `requirements.txt` (Python)
   - `package.json` (Node.js)
   - 不包含锁定文件（或根据团队约定）

5. **配置文件**
   - `.gitignore`
   - `.editorconfig`
   - `pyproject.toml`
   - `Makefile`
   - `docker-compose.yml`

---

## 🔧 使用建议

### 1. 已经上传了不该上传的文件？

**从Git历史中删除**:

```bash
# 从Git仓库中删除文件，但保留本地文件
git rm --cached node_modules/ -r
git rm --cached .idea/ -r
git rm --cached *.onnx

# 提交更改
git commit -m "Remove ignored files from Git history"

# 如果已经push到远程，需要force push（危险操作！）
git push -f origin main
```

**注意**: Force push会改写历史，团队协作时需要通知所有成员。

### 2. 检查哪些文件会被提交

```bash
# 查看将要提交的文件
git status

# 查看未被忽略的文件
git ls-files
```

### 3. 测试 .gitignore 规则

```bash
# 检查某个文件是否被忽略
git check-ignore -v node_modules/

# 检查所有被忽略的文件
git status --ignored
```

### 4. 全局 .gitignore（可选）

为所有Git项目设置全局忽略规则：

```bash
# 创建全局配置
git config --global core.excludesfile ~/.gitignore_global

# 编辑全局忽略文件
cat >> ~/.gitignore_global << EOF
.DS_Store
.idea/
*.swp
EOF
```

---

## 📚 相关资源

- [GitHub官方 .gitignore模板](https://github.com/github/gitignore)
- [Python .gitignore模板](https://github.com/github/gitignore/blob/main/Python.gitignore)
- [Node.js .gitignore模板](https://github.com/github/gitignore/blob/main/Node.gitignore)
- [Git LFS文档](https://git-lfs.github.com/) - 用于大文件版本控制

---

## 🤝 团队协作建议

1. **统一配置**: 团队成员使用相同的 `.gitignore`
2. **及时更新**: 发现新的需要忽略的文件类型时，及时更新并通知团队
3. **代码审查**: PR时检查是否误提交了敏感文件
4. **自动化检查**: 使用pre-commit hook自动检查（如detect-secrets）

---

最后更新: 2025-10-12
