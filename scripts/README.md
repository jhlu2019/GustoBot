# GustoBot 工具脚本

本目录包含GustoBot项目的各种辅助脚本。

## 📁 脚本列表

### 1. check-gitignore.sh / check-gitignore.bat

**用途**: 检查Git忽略规则是否正确配置

**支持平台**:
- `check-gitignore.sh` - Linux / macOS
- `check-gitignore.bat` - Windows

**功能**:
- ✅ 验证常见文件类型是否被正确忽略
- ✅ 检测已跟踪的大文件（>1MB）
- ✅ 扫描敏感文件（密钥、证书等）
- ✅ 统计被忽略的文件数量

**使用方法**:

**Linux/macOS**:
```bash
# 添加执行权限（首次运行）
chmod +x scripts/check-gitignore.sh

# 运行检查
./scripts/check-gitignore.sh
```

**Windows**:
```cmd
# 直接运行
scripts\check-gitignore.bat
```

**输出示例**:
```
======================================
GustoBot .gitignore 检查工具
======================================

✅ Git仓库检查通过

📋 检查常见忽略规则...
======================================
✅ node_modules/ - 已忽略
✅ .idea/ - 已忽略
✅ *.onnx - 已忽略
✅ *.log - 已忽略
✅ .env - 已忽略
✅ proxies.txt - 已忽略
✅ cookies.json - 已忽略
✅ data/ - 已忽略
✅ *.db - 已忽略
✅ *.sqlite - 已忽略

======================================

📊 检查已跟踪的大文件（>1MB）...
======================================
✅ 没有发现大于1MB的已跟踪文件

======================================

🔒 检查敏感文件...
======================================
✅ 没有发现敏感文件

======================================

📈 被忽略的文件统计...
======================================
被忽略的文件数: 156

部分被忽略的文件（最多显示20个）:
  - node_modules/
  - .idea/
  - data/chroma/
  - logs/
  ...

======================================

✅ 所有检查通过！.gitignore配置正确。
```

---

## 🔧 常见问题

### Q1: 发现大文件被跟踪怎么办？

**从Git中移除，但保留本地文件**:
```bash
git rm --cached 大文件.onnx
git commit -m "Remove large file from Git"
```

**如果已经push到远程，需要清理历史**:
```bash
# 使用git-filter-repo（推荐）
pip install git-filter-repo
git filter-repo --path 大文件.onnx --invert-paths

# 或使用BFG Repo-Cleaner
# 下载 bfg.jar
java -jar bfg.jar --delete-files 大文件.onnx
```

### Q2: 发现敏感文件被提交怎么办？

⚠️ **紧急处理**:

1. **立即撤销密钥**（如果是API密钥、密码等）
2. **从Git历史中删除**:
```bash
git filter-repo --path .env --invert-paths
```
3. **更改所有相关密钥**
4. **Force push更新远程仓库**（需通知团队）:
```bash
git push -f origin main
```

### Q3: node_modules/ 已经被提交了怎么办？

```bash
# 1. 从Git中删除（保留本地文件）
git rm -r --cached node_modules/

# 2. 确保.gitignore包含node_modules/
echo "node_modules/" >> .gitignore

# 3. 提交更改
git add .gitignore
git commit -m "Remove node_modules from Git tracking"

# 4. Push到远程
git push origin main
```

### Q4: .idea/ 目录已经被跟踪了怎么办？

```bash
# 1. 从Git中删除
git rm -r --cached .idea/

# 2. 确保.gitignore包含.idea/
# （已经在.gitignore中）

# 3. 提交更改
git commit -m "Remove .idea directory from Git tracking"

# 4. Push
git push origin main
```

---

## 🛠️ 开发新脚本

如果需要添加新的工具脚本，请遵循以下规范：

### 脚本命名规范

- **Linux/macOS**: `script-name.sh`
- **Windows**: `script-name.bat`
- **Python**: `script_name.py`

### 脚本结构

```bash
#!/bin/bash
# 脚本用途简述
# 作者: Your Name
# 日期: 2025-01-01

# 使用说明
function usage() {
    echo "用法: $0 [options]"
    echo "选项:"
    echo "  -h, --help     显示帮助信息"
    exit 1
}

# 主逻辑
function main() {
    # 实现代码
    echo "Hello, World!"
}

# 入口点
main "$@"
```

### 文档要求

每个脚本都应该在本README中添加说明，包括：
- 脚本用途
- 支持平台
- 使用方法
- 输出示例
- 常见问题

---

## 📚 相关文档

- [.gitignore配置说明](../.gitignore.md)
- [主README](../README.md)
- [Git官方文档](https://git-scm.com/doc)

---

## 🤝 贡献

欢迎贡献新的工具脚本！请确保：
1. 脚本有清晰的注释
2. 添加使用文档
3. 测试通过
4. 遵循命名规范

---

最后更新: 2025-10-12
